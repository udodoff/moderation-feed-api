Для запуска проекта необходимо наличие на машине установленных php, composer

Для установки зависимостей перейти в корневую директорию проекта, в терминале запустить команду:
    composer install

Для запуска проекта в терминале запустить команду:
    symfony server:start